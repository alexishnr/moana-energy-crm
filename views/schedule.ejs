<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Programmer un rendez-vous</title>
</head>
<body>
    <div class="content clearfix">
        <div class="intro-title-dark-bg">
            <div class="row">
                <div class="column width-10 offset-1 center">
                    <div class="title-container">
                        <div class="title-container-inner color-white">
                            <h1 class="title-large mb-30">Programmer un rendez-vous</h1>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="container">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="/">Accueil</a></li>
                    <li class="breadcrumb-item"><a href="/clients">Liste des clients</a></li>
                    <li class="breadcrumb-item"><a href="/client/<%= client.id %>">Fiche client</a></li>
                    <li class="breadcrumb-item active" aria-current="page">Programmer un rendez-vous</li>
                </ol>
            </nav>
        </div>
        <section class="section-block">
            <div class="row">
                <div class="column width-12">
                    <% if (typeof success !== 'undefined') { %>
                        <% if (success) { %>
                            <div class="toastBox alertingGreen">
                                Action effectuée avec succès
                            </div>
                        <% } else { %>
                            <div class="toastBox alertingRed">
                                Erreur lors de l'action
                            </div>
                        <% } %>
                    <% } %>
                    <form action="/client/<%= client.id %>/schedule" method="POST">
                        <label for="date">Date:</label>
                        <input type="date" id="date" name="date" required>
                        
                        <label for="time">Heure:</label>
                        <input type="time" id="time" name="time" required>
                        
                        <button type="submit" class="cta-button" style="margin-top: 30px;">Programmer</button>
                    </form>
                </div>
            </div>
        </section>
    </div>
</body>
</html>